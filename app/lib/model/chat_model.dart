class ChatModel {
  final String? name;
  final String? icon;
  final bool? isGroup;
  final String? time;
  final String? currentMessage;
  ChatModel(
      {this.name, this.icon, this.isGroup, this.time, this.currentMessage});
}
